# Substrings exempted from the mirror pre-flight secret scan
# (scripts/mirror-secret-preflight.sh). One per line. A history line containing
# any of these is not treated as a credential.
#
# Only add a value that is provably synthetic, and say why. Anything real must be
# purged from history instead, never allowlisted.

# The shipped DEFAULT for JWTSecret when an operator has not set one. It is not a
# credential, it is the string that tells them to set one, and the server refuses
# to start in production while it is still in place.
change-me-in-production

# Go handler/config tests. Long enough to satisfy the 32-byte signing-key check.
test-jwt-secret-1234567890-abcdef
this-is-a-strong-32-byte-secret-value-here
test-secret-32-bytes-x-x-x-x-x-x

# Playwright end-to-end fixtures. The stack is booted with throwaway values and
# each one names itself as e2e / not-for-production.
e2e-jwt-secret-not-for-production
e2e-crm-hmac-secret

# E2E workspace-invite fixture: the invited member's throwaway password.
invitee-pw-123456
